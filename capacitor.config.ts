import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.authmanagerangularui',
  appName: 'authmanagerangularui',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
